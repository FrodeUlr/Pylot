[package]
name = "pylot"
version = "0.5.0"
edition = "2021"
authors = ["Fulrix"]

[dependencies]
shared = { path = "../shared" }
tui = { path = "../tui" }
clap = { version = "4.5", features = ["derive"] }
colored = "3.0"
tokio = { version = "1.47", features = ["full"] }
color-eyre = "0.6"

[dev-dependencies]
assert_cli = "0.6"
shellexpand = "3.1"
